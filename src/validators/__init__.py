"""Independent validator harness: physical bounds, cross-curve, model-mismatch.

Fixed, versioned, external to the agents. The agents execute these validators; they
never choose which checks fire or author new ones (Charter Invariant 3).
"""
