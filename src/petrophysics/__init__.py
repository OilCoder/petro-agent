"""Vetted deterministic petrophysical engine.

Every function here is frozen, version-pinned, and golden-tested. The LLM never
authors or edits these equations at runtime — it only selects and parameterizes them.
"""
