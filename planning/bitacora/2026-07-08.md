# Bitácora — 2026-07-08

## Sesión — Informes por pozo + 5 visuales nuevos en el sitio

### Cambios técnicos
- **Informes particulares de pozo publicados** (petición del usuario: "veo los generales
  pero no los particulares"): 71 pozos del summit (25 clase A + 46 clase B vintage, 522
  figuras, ~32 MB) en `docs/reports/wells/` + los pozos restantes de opus/glm/ultra (8).
  El builder ya no BORRA los links por-pozo del informe de campo: los reescribe a las
  páginas publicadas. Corrección de UX del usuario a mitad: los pozos se navegan DESDE el
  informe general (no recuadros sueltos) → se publicaron también los informes de campo de
  cada agente (opus_field/glm_field/ultra_field, con sus pozos enlazados dentro) y cada
  ficha enlaza "su informe de campo →". Catálogo `reports/pozos.html` accesible desde los
  informes generales. qwen-thinking sin per-pozo (artefacto de intentos previos; nota de
  honestidad en el catálogo).
- **5 visuales nuevos** (SVG inline con tokens, bilingües, light/dark): calibración VOLVE
  (index §4: r=0.96/0.91/0.87 + cobertura P10–P90 antes/después 35/1.8/31→95/98/88%);
  flujograma del loop con la frontera modelo/motor (reemplaza el ASCII de ingeniería);
  matriz de thinking config×modelo (zonas ≥700 m); barras de precisión de zona (opus 1.00
  con banda 0.68–1.00; par gpt-5 0.77→0.00); explicador del overburden (RHOB por tercios
  1.77/1.74/2.49 + PHIE 0.229→0.069, pay 296→31 m).

### Verificación
- check_links: 0 rotos en 93 páginas; figuras 34+522; browser: catálogo, pozo clase A
  (O'Brate No. 3 completo con QC), field→pozo, los 5 charts en dark, ES/EN.

### Errores
- Los artefactos del conversor md→HTML reaparecieron una 3ª vez — ahora generados por MI
  PROPIA bitácora (las sesiones que documentaban el fix citaban los patrones literales en
  backticks, y el conversor los reconvertía). Fix de raíz: `_inline()` ahora protege el
  contenido de los code spans con placeholders antes de procesar links/imágenes.
- Tres SVG salieron con texto cortado en el borde del viewBox (título derecho de VOLVE,
  columna opus de la matriz, pie del flowchart, título del overburden) — se notó solo en
  browser. Lección: con viewBox fijo, presupuestar el ancho ANTES (n_cols × cw + margen).

## Sesión 2 — Revisión editorial del usuario (9 items) + el campo-trampa

### Cambios técnicos (docs/)
- **APIs completos y buscables**: toda mención de pozo usa el API completo sin comas
  (15-135-24881, no "24,881" ni "(25945)") — para buscarlos en la data pública de KGS.
  Catálogo y badges normalizados igual.
- **Metodología re-narrada**: el pipeline cuenta el viaje del pozo en 5 pasos numerados
  ("Llega el pozo → El agente analiza → El motor calcula → La física objeta → El agente
  redacta"), cada uno con una frase de qué hace.
- **"vetados" eliminado** (en ES connota "prohibidos") → "métodos validados"; EN conserva
  "vetted", que sí es idiomático.
- **Chart A/B con leyenda de eje**: "elecciones interpretativas por pozo (mediana del batch)".
- **"Ronda" → "Fase experimental"** en tabla de evolución y títulos relacionados.
- **Tabs**: "Ingeniero petrofísico" / "Ingeniero de software".
- **Iconos en las 3 narrativas**: Lucide (gota/código/cuaderno, ISC) inline con
  currentColor → toman el acento de página y se adaptan a light/dark.
- **Informes reorganizados**: informe de campo del summit como tarjeta principal + los 4
  comparables en desplegable. Distinción explícita: la vara la escribió **claude-fable-5**
  (techo CONTAMINADO); el agente evaluado es claude-opus-4.8 — mismo logo, roles opuestos.
- **El campo-trampa** (petición): callout en petrolero#campo + resumen en index — Schaben
  se eligió PORQUE engaña al principiante con el net pay (2/3 de columna con "porosidad"
  0.35–0.40 que parece cientos de metros; el real es 2–13 m profundo): no se aprueba
  calculando bien sino analizando bien; verificable en KGS; moderna+vintage; público.

### Errores
- El reinicio del PC borró /tmp con los fragmentos fuente del sitio y sus builders. Se
  notó al intentar editarlos. Los HTML construidos viven en git → esta pasada se editó
  directo sobre docs/ y se recreó check_links en el scratchpad. Lección: los generadores
  del sitio deben vivir en debug/ (repo) como dbg_build_site_pages.py — no en /tmp.
- El barrido de comas alcanzó un src de imagen cuyo ARCHIVO sí lleva coma (crossplot de
  24,881) — lo atrapó el check de links; restaurado.

## Sesión 3 — provenance→trazabilidad + informes por familia

- "provenance" (ES) → "trazabilidad" (index + petrolero); EN conserva provenance.
- Informes finales por familia (petición): informe de campo principal + TRES desplegables
  (claude-opus-4.8·Anthropic / glm-5.2·Z.ai / nemotron-ultra·NVIDIA), cada uno con su logo
  en el summary; la vara (fable) en 24881 vive dentro del de opus para comparar mano a mano.
- BUG raíz de las fichas rotas del screenshot del usuario: anchors anidados (link "su
  informe de campo →" dentro de una tarjeta <a>) — el navegador partía la ficha en pedazos.
  Fix: esas tarjetas son <div> con el título como link.
- Gotcha de shell: `pkill -f "http.server 8931"` matchea la PROPIA línea de comando del
  tool y se mata a sí mismo (exit 144, muere la cadena entera). Usar `fuser -k 8931/tcp`.

## Sesión 4 — Coherencia texto↔figura y chart de ventanas verificado

- Hero petrolero: "¿Puede una IA leer la roca?" → "la formación".
- Catch del usuario en el composite: el caption pedía mirar la densidad del tramo somero,
  pero la figura del summit NO tiene curvas someras (el análisis las excluyó). Caption
  reescrito para explicar lo que sí se ve: "las curvas empiezan en ~900 m — esa es la
  decisión, dibujada", remitiendo al panel del overburden para la columna completa.
- Chart de ventanas RECONSTRUIDO con datos verificados: el anterior mezclaba pozos (cada
  barra de un pozo distinto contra una banda de otro) y la barra de qwen-think no
  correspondía al ledger. Nuevo diseño: TODAS las configs sobre el MISMO pozo
  (15-135-24881), banda del summit + precisión por barra, ordenado; caption con lectura
  por modelo (opus 1.00 aprieta dentro; ultra 0.68; qwen-think 0.62 arrastra esponja
  desde 600; glm 0.40 aquí pero 0.79 en 25990; super 0.38 no selecciona; gpt-5 0.00 —
  y su 1.00 en 25399 vía reintento). Generador permanente: debug/dbg_gen_chart_windows.py
  (lección aplicada: generadores en debug/, no en /tmp).

## Sesión 5 — Auditoría del chart de precisión (a raíz del bug del de ventanas)

El usuario preguntó si el chart "leaderboard en una barra" sufría el mismo problema.
Verificado con `debug/dbg_verify_precision_chart.py` (recomputa por pozo desde ledgers):
los 7 valores coinciden exactamente — NO está afectado, porque su métrica siempre fue
pozo-contra-su-propio-pozo (mediana del batch), no una ventana única. Se aclaró el
caption para distinguirlo del chart de un-solo-pozo de arriba.
